#include <iostream>
#include "StackType.h"

using namespace std;
int main()
{
	int n=6;
    stackType<int> a1(n);
	int x,y,z;
    while(!)
  
    while(!a1.isEmptyStack()){
	cout<<a1.top()<<endl;
    a1.pop();
    }
    return 0;
}
